# 全局约束文档

## 1. 项目概述

本项目是一个多模块教程项目，包含以下主要模块：

- **nerf**: Neural Radiance Fields (NeRF) 神经辐射场教程
- **robotwin**: RoboTwin2.0 双臂机器人操作教程
- **slam**: Simultaneous Localization and Mapping (SLAM) 教程
- **3d_reconstruction**: 3D重建技术教程
- **control_planning**: 机器人控制与规划教程（运动规划/最优控制/伺服控制）

## 2. 项目分类结构

项目按资源类型分为三大类，每类内部按主题（nerf、robotwin、slam、3d_reconstruction）分子目录：

```
RoboTwinTutorial/
├── papers/                 # 论文库：论文源码 / PDF
│   └── <主题>/
├── tutorials/              # 教程文档：中文分章教程
│   └── <主题>/
│       ├── README.md       # 主题导航入口
│       ├── OVERVIEW.md     # 主题总览（论文/教程/代码关系）
│       └── 01_*.md         # 章节，编号排序
└── projects/               # 代码项目：可运行参考实现
    └── <主题>/
        ├── README.md       # 项目说明与快速开始
        ├── *.py
        └── tests/
```

新增主题时三处联动：先 `papers/<主题>/`（论文）→ 再 `tutorials/<主题>/`（教程）→ 最后 `projects/<主题>/`（可选代码），并更新各类索引 README。

```
projects/<主题>/
├── README.md       # 项目说明与快速开始
├── TUTORIAL.md     # 零基础代码导读（必配，见 §4.4）
├── DEBUG.md        # 调试与测试教程（必配，见 §4.5）
├── METRICS.md      # 测评指标教程（必配，见 §4.7）
├── metrics.py      # 统一测评模块（§4.7）
├── *.py
└── tests/
```

## 3. 文档规范

### 3.1 Markdown 格式要求

- **文件命名**: 使用中文命名，下划线分隔，格式 `数字_标题.md`
- **章节编号**: 使用 `#`、`##`、`###` 等标题层级，确保层级清晰
- **代码块**: 使用三个反引号 ``` 包裹，指定语言
- **链接**: 使用相对路径引用其他文档
- **图片**: 使用相对路径，建议格式 `![描述](path/to/image.png)`

### 3.2 内容要求

每个教程章节应包含：
1. **概述**: 简要介绍本章内容
2. **背景知识**: 相关概念和基础知识
3. **核心内容**: 详细讲解——其中每个核心知识点必须按 §3.4 的"五步法"展开
4. **代码示例**: 如有代码，提供完整示例
5. **总结**: 本章要点回顾

### 3.3 语言规范

- 使用中文撰写，保持学术性和可读性
- 专业术语首次出现时提供英文对照
- 公式使用 LaTeX 格式：`$E = mc^2$` 或 `$$...$$`

### 3.4 论述结构规范（问题导向五步法）

教程中的每个核心知识点，必须按以下五步展开。动机：读者只有先理解"为什么需要它"，
才能真正理解"它是什么"；只有看到完整推导，才能在遇到变体问题时自行重建结论。

| 步骤 | 写什么 | 合格标准 |
|------|--------|----------|
| ① 问题场景 | 该知识点要解决的真实问题 | 具体到"手头有什么、缺什么、想要什么"；禁止以抽象定义或结论开头 |
| ② 解决方法 | 解决方案是什么 | 给出可操作的做法；存在多种候选方案时逐一列出 |
| ③ 选型理由 | 为什么这么解决 | 与至少一个替代方案对比：优点、代价、为何在本场景下值得 |
| ④ 理论依据 | 用到什么理论 | 点名理论及其出处（论文 / 教材 / 定理名） |
| ⑤ 完整推导 | 理论的推导过程 | 从已知出发逐步推出，无跳步；每步变换注明依据（定义、公理或前一步结论） |

**硬性要求**：

1. 五步缺一不可；纯操作性内容（如环境安装、命令用法）可豁免，但须在开头注明"操作步骤，不含理论"。
2. 推导中禁止出现"易得""显然""读者可自行验证"——凡跳步必须补全，或明确引用出处。
3. 推导中的公式应编号或可定位，供后续章节与代码回引（如 `09_代码逐行精读.md` 回引 Eq.3）。
4. 代码章节回引理论时，须指出对应到推导的哪一步，不能只丢一个公式编号。
5. 该结构同样适用于解释设计决策（如"为什么用分层采样""为什么 coarse/fine 双网络"）。

**示范**（以本项目主线 NeRF 的体积渲染为例，写作时可对照此深度）：

> **① 问题场景**：手头有一组带相机位姿的照片，想合成没拍过的新视角图像。传统做法需要先重建
> 显式几何（网格或点云），但视角稀疏、物体半透明时重建质量差，渲染出明显伪影。
>
> **② 解决方法**：把场景表示为连续的 5D 辐射场（位置+方向 → 颜色+密度），对每条相机光线做
> 体积渲染积分，直接优化神经网络使渲染图逼近真实照片。
>
> **③ 选型理由**：相比显式几何 + 纹理贴图，连续场无需重建几何、天然支持半透明与软边缘，
> 且渲染过程可微，能端到端用梯度下降；代价是每像素要积分数十至上百次网络查询，训练与渲染
> 都更慢——本研究场景精度优先于速度，值得。
>
> **④ 理论依据**：辐射度量学中的体积渲染方程（Max, *"Optical Models for Direct Volume
> Rendering"*, IEEE TVCG 1995）。
>
> **⑤ 完整推导**：
> 相机光线参数化为 $\mathbf{r}(t)=\mathbf{o}+t\mathbf{d}$，穿过介质。
> 微分段 $[t, t+\mathrm{d}t]$ 对颜色的贡献 = 三因子相乘：到达该处未被吸收的能量比例
> $T(t)$、该处被散射进光线的比例 $\sigma(t)\mathrm{d}t$（密度×段长）、该处辐射 $c$。
> 透射率的微分方程：穿过微分段后能量按比例衰减，
> $$\mathrm{d}T(t) = -\sigma(t)\,T(t)\,\mathrm{d}t$$
> （依据：概率论中独立吸收事件的指数衰减模型，$\sigma\,\mathrm{d}t$ 即被吸收概率）。
> 解此微分方程（依据：一阶线性 ODE 分离变量）得
> $$T(t) = \exp\!\Big(-\int_0^t \sigma(s)\,\mathrm{d}s\Big).$$
> 把各微分段的贡献沿光线累加（依据：线性叠加原理）：
> $$C(\mathbf{r}) = \int_{t_n}^{t_f} T(t)\,\sigma\big(\mathbf{r}(t)\big)\,\mathbf{c}\big(\mathbf{r}(t),\mathbf{d}\big)\,\mathrm{d}t. \tag{Eq.3}$$
> 数值实现时把 $[t_n, t_f]$ 分为 $N$ 段、段内取 $\sigma$ 为常数（依据：分层采样近似积分），
> 记段长 $\delta_i$，则 $T_i = \exp(-\sum_{j<i}\sigma_j\delta_j)$（前一步 $T(t)$ 的离散化），
> 得离散形式：
> $$C(\mathbf{r}) = \sum_{i=1}^{N} T_i\,\big(1-\mathrm{e}^{-\sigma_i\delta_i}\big)\,\mathbf{c}_i.$$
> 即 NeRF 论文 Eq.3；`projects/nerf/nerf/render.py` 中的 `volume_render()` 与此逐项对应。

## 4. 代码规范

### 4.1 Python 代码规范

- 遵循 PEP 8 编码规范
- 使用类型提示 (Type Hints)
- 添加必要的注释和文档字符串
- 使用有意义的变量和函数名

### 4.2 代码组织

- 代码文件放在 `code/` 目录下
- 每个模块的代码文件命名清晰
- 提供完整的可运行示例

### 4.3 注释规范（中文，硬性要求）

- **所有代码**（Python 模块 docstring、类与函数 docstring、关键行内注释）一律使用**中文**撰写；专业术语、LaTeX 公式与论文/教程式号引用可保留英文与符号。
- **模块 docstring**：开头给出**函数流水线**——本模块在整体系统中的位置、输入/输出、与其他模块的调用依赖关系（如 `photoba` 依赖 `core.solver` 与 `direct` 的场景合成）。
- **函数/方法 docstring**：中文说明作用与数学含义（引用论文式号 / 教程式号）；`Args` / `Returns` 逐项中文说明，含**变量含义、形状、单位/取值范围**。
- **类与实例属性**：在类 docstring 或 `Attributes` 段给出中文含义、形状约定与取值范围。
- **算法体**：关键步骤加行内中文注释，标注对应推导的哪一步（如"依据：教程 (8.5) 正规方程"）。
- 改动既有代码时同步维护注释；新增第三方接口调用处需注明其返回值含义。

### 4.4 零基础代码导读（TUTORIAL.md，硬性要求）

每个 `projects/<主题>/` 必须包含一份**以零基础读者为基准**的代码导读文档 `TUTORIAL.md`，
说明**代码的作用和原理、目标输入和输出、数据结构、计算公式**等。零基础基准 =
不假设读者有机器人学/深度学习背景，术语首现给中文通俗解释，但公式仍须与
`tutorials/<主题>/` 章节的式号互引（深度材料留给教程，导读只讲"代码里发生了什么"）。

**必备结构**（按序）：

1. **这个项目做什么**：一句话 + 代码总体作用与原理（对应 tutorials/ 哪几章）。
2. **环境与运行**：从零安装到跑通，每条命令附预期输出（含运行时长量级）。
3. **目标输入与输出**：每个入口脚本 / 核心类的输入格式（文件、数组——形状/单位/取值范围）与输出产物；给出"最小可运行示例"。
4. **数据结构**：核心类/数组/dict 的字段、形状、含义、取值范围（表格逐项列出）。
5. **计算公式**：代码中每处关键计算对应的数学公式，标注教程式号（如 (3.1)）与代码位置（`文件:行` 或函数名）。
6. **函数流水线**：从入口到输出的调用链（mermaid `flowchart` 或缩进文字图），与 §4.3 模块 docstring 的流水线一致。
7. **常见问题**：典型报错与修复、参数怎么调。

**质量要求**：文档中的形状/公式/函数名必须与代码实际一致（写前通读代码、写后运行
"最小可运行示例"验证）；代码更新时同步更新 TUTORIAL.md。

### 4.5 调试与测试教程（DEBUG.md + launch.json，硬性要求）

每个 `projects/<主题>/` 必须包含调试与测试教程 `DEBUG.md`，并在根 `.vscode/launch.json`
中为该项目提供调试入口。

**launch.json 要求**：
- 每个项目至少三条配置：**核心入口调试**（主脚本冒烟）、**全局测试**（pytest 或全套直跑）、
  **单点测试**（当前测试文件 + 可选测试名过滤参数，用 `inputs` 交互输入）。
- 每条配置注明正确的解释器环境（nerf 用 conda `llm_env`，slam 用系统 python3 即可）与 `cwd`。

**DEBUG.md 必备结构**（按序）：

1. **环境与两种测试**：本项目用哪个解释器；**单点测试**（跑单个测试函数：
   `python tests/test_X.py <测试名子串>` 直跑过滤 / `pytest tests/test_X.py::test_name -v`）
   与**全局测试**（`pytest tests/ -v` 或逐文件直跑）的命令、适用场景（改了一个模块→单点；
   提交前→全局）。
2. **VS Code 断点调试**：launch.json 各配置怎么选、在哪里打断点（给出推荐断点位置：
   函数名 + 行为说明）、`justMyCode` 何时关闭。
3. **重点观察变量表**（核心章节）：按模块列出——断点位置、**变量名、含义、形状、
   健康值范围、异常信号**（如 fastslam 调试时看 `N_eff`、权重分布是否塌缩；
   vins 调试看残差范数是否 ~O(1)、尺度比是否趋于 1）。
4. **常见调试场景**：2-4 个"症状 → 断点位置 → 看什么 → 结论"的实战案例。
5. **测试怎么写**：新测试的命名（`test_<行为>`）、deterministic seed、运行时长预算。

**质量要求**：变量表中的名字/形状/健康值必须与代码一致；每次代码变更涉及被监视
变量时同步更新 DEBUG.md。

### 4.6 开发环境配置清单（Anaconda，硬性要求）

- **全项目统一使用 Anaconda 管理环境**，主环境为 `llm_env`（python 3.10 / torch / numpy / pytest）。
- 仓库根目录维护 `environment.yml`：环境配置清单。新增依赖必须同步该文件，
  并在对应项目 README 的"快速开始"注明本项目额外依赖。
- 每个项目 README 的快速开始必须给出三段：`conda activate llm_env` →
  依赖安装/校验命令 → **验证命令**（import 成功并打印版本号）。
- 解释器分工在 DEBUG.md 写明：projects/nerf（依赖 torch）用 `llm_env`；
  projects/slam（纯 numpy）用任意含 numpy≥1.26 的环境（`llm_env` 亦兼容）。

### 4.7 测评指标与指标教程（metrics.py + METRICS.md，硬性要求）

每个 `projects/<主题>/` 必须提供**统一测评模块**与**指标教程**：

- **metrics.py**：全部指标实现为纯函数（类型提示 + §4.3 中文注释），
  tests/ 中每个指标至少一个测试（含与朴素实现的交叉验证）；评估命令可独立运行。
- **METRICS.md**：以零基础读者为基准，**每个指标一节**，固定四要素：
  1. **作用**——衡量什么质量、为什么需要它、对应论文/教程的哪部分主张；
  2. **如何计算**——数学公式（编号）+ 代码位置（`metrics.py` 函数名）+
     与 `tutorials/<主题>/` 式号互引 + 文献出处（如 ATE 按 Sturm et al., 2012 定义）；
  3. **健康值范围**——本项目各模块的实测参考值与异常信号；
  4. **如何运行**——评估命令示例与输出解读。
- **本项目指标基线**：nerf = PSNR / SSIM（LPIPS 列为延伸）；slam = ATE RMSE
  （Umeyama 对齐）、旋转误差、尺度比、深度/地图 RMSE；control_planning =
  规划成功率 / 路径长度 / 轨迹代价（模块建立时补齐）。

### 4.8 VS Code 统一配置（硬性要求）

- **本项目完全使用 VS Code 开发与测试**，`.vscode/` 是唯一配置入口，四个文件统一维护：
  - `settings.json`：**Anaconda 环境写入 VS Code**——`python.defaultInterpreterPath`
    指向 conda `llm_env` 的 python、终端自动激活、pytest 测试发现（`pytestArgs`）
    与智能分析路径（`extraPaths`/`PYTHONPATH`）必须**覆盖全部项目**；
  - `launch.json`：按 §4.5（每项目核心入口 / 全局测试 / 单点测试三条起）；
  - `tasks.json`：单点与全局测试任务（`group: test`）、常用运行任务；
  - `extensions.json`：推荐扩展（Python / Pylance / debugpy / black-formatter）。
- **新增项目**必须在以上四处同步注册（照抄现有项目的条目改路径）。
- 配置教程 `.vscode/SETUP.md`：解释器选择、测试三种跑法、调试用法、常见问题；
  配置结构变更时同步更新该教程。

## 5. 提交规范

### 5.1 Git 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**:
- `docs`: 文档更新
- `feat`: 新功能
- `fix`: Bug 修复
- `refactor`: 代码重构
- `style`: 代码格式调整
- `test`: 测试相关
- `chore`: 构建/工具链相关

**Scope 范围**:
- `nerf`: NeRF 模块
- `robotwin`: RoboTwin 模块
- `slam`: SLAM 模块
- `3d_reconstruction`: 3D重建模块
- `control_planning`: 控制与规划模块
- `global`: 全局配置

### 5.2 提交频率

- **每日**: 提交当日完成的文档或代码
- **每周**: 进行代码审查和文档整理
- **里程碑**: 完成模块后进行正式提交

### 5.3 分支管理

- `main`: 主分支，保持稳定
- `dev`: 开发分支
- `feature/*`: 功能分支

## 6. 文档与代码分离

### 6.1 原则

- 文档和代码分开存储，互不干扰
- 文档侧重讲解原理和思路
- 代码侧重实现细节和示例
- 文档引用代码时使用相对路径

### 6.2 目录结构

```
papers/         # 论文资料（源码、PDF）
tutorials/      # 教程文档（纯 Markdown）
projects/       # 代码项目（可运行实现）
```

## 7. 质量检查

### 7.1 文档检查清单

- [ ] 文件命名规范
- [ ] 章节编号正确
- [ ] 代码示例可运行
- [ ] 链接有效
- [ ] 格式统一
- [ ] 内容完整
- [ ] 核心知识点含五要素：问题场景、解决方法、选型理由、理论依据、完整推导（§3.4）
- [ ] 推导无跳步，每步变换注明依据

### 7.2 代码检查清单

- [ ] 通过 PEP 8 检查
- [ ] 类型提示完整
- [ ] 文档字符串齐全
- [ ] 无语法错误
- [ ] 可直接运行

## 8. 版本控制

### 8.1 版本号规范

使用语义化版本号：`MAJOR.MINOR.PATCH`

- `MAJOR`: 不兼容的 API 修改
- `MINOR`: 向下兼容的功能新增
- `PATCH`: 向下兼容的问题修正

### 8.2 变更日志

在项目根目录维护 `CHANGELOG.md`，记录所有重要变更。

## 9. GitHub 集成

### 9.1 自动化工作流

- 定期自动提交（每日/每周）
- 代码质量检查
- 文档生成

### 9.2 Issue 管理

- 使用 GitHub Issues 跟踪问题
- 每个 Issue 指定负责人
- 定期清理已解决的问题

## 10. 团队协作

### 10.1 角色分工

- **文档负责人**: 负责文档编写和质量检查
- **代码负责人**: 负责代码实现和测试
- **审核人员**: 负责审查提交内容

### 10.2 协作流程

1. 提交前进行自检
2. 提交 Pull Request
3. 代码审查
4. 合并到主分支

## 11. 参考资源

- [Markdown 基础语法](https://markdown.com.cn/)
- [PEP 8 编码规范](https://pep8.org/)
- [Git 提交规范](https://www.conventionalcommits.org/)
- [语义化版本](https://semver.org/lang/zh-CN/)
